#include "DE0_CV.inc"
module DE0_CV{
  mem m[32][8];
  reg pointer[5];

  reg in_r[8] = 0;

  is_empty = if(pointer == 0) 1 else 0;

	func pop{
    front_p++;
		out = m[front_p];
	}

	func push seq{
    seq{
      in_r := in;
      {
        m[end_p] := in_r;
        end_p++;
      }
    }
  }
}
